'use strict';

/**
 * sponsor-registration router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::sponsor-registration.sponsor-registration');
